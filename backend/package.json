{
  "name": "kitsune-capture-backend",
  "version": "1.0.0",
  "private": true,
  "description": "AWS SAM backend for Kitsune image capture analysis via Gemini 2.5 Flash.",
  "main": "src/app.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.883.0",
    "@aws-sdk/lib-dynamodb": "^3.883.0",
    "@google/genai": "^1.15.0"
  }
}
